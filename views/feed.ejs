<%- include ('./partials/header.ejs') -%>
<div class="w-full min-h-screen bg-zinc-800 px-10 py-8 text-white">
    <div class="cards flex flex-wrap gap-2">

        <% posts.forEach(function(elem){ %>
            <div class="card w-32 overflow-hidden">
                <div class="w-full h-40 bg-zinc-700 rounded-lg overflow-hidden">
                    <img class="w-full h-full object-cover" src="/images/uploads/<%= elem.image %>" alt="">
                
                </div>
                <!-- <h2 class="mt-2 text-1xl font-bold"><%= elem.title %></h2>
                <h2 class="mt-2 text-sm"><%= elem.description %></h2>
                <h6 class="text-xs opacity-500 font-semibold  text-blue-500 " >@<%= elem.user.username %></h6> -->
    
            </div>

       <% })%>

        

    </div>

</div>
<%- include ('./partials/footer.ejs') -%>